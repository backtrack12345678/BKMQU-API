import { MidtransInterceptor } from './midtrans.interceptor';

describe('MidtransInterceptor', () => {
  it('should be defined', () => {
    expect(new MidtransInterceptor()).toBeDefined();
  });
});
